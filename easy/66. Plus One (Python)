class Solution(object):
    def plusOne(self, digits):
        """
        :type digits: List[int]
        :rtype: List[int]
        """

        integer = ""
        for i in range(len(digits)):
            integer = integer + str(digits[i])
        num = int(integer)+1
        return [int(i) for i in str(num)]
