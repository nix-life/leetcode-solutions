class Solution(object):
    def findDisappearedNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        setNums = set(nums)

        n = []

        for i in range (1, len(nums) + 1):
            if i not in setNums:
                n.append(i)

        return n
